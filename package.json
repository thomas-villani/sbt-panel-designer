{
  "name": "pd3",
  "private": true,
  "description": "Maxpar Panel Designer v3 - root scripts (no workspaces: engine/ and web/ install separately)",
  "scripts": {
    "test": "npm run test:etl && npm run test:engine && npm run test:web",
    "test:etl": "cd etl && uv run pytest -q",
    "test:engine": "npm --prefix engine test",
    "test:web": "npm --prefix web test",
    "e2e": "npm --prefix web run e2e",
    "typecheck": "npm --prefix engine run typecheck && npm --prefix web run typecheck",
    "validate": "npm --prefix engine run validate",
    "data": "cd etl && uv run pd3-etl instruments && uv run pd3-etl catalog && uv run pd3-etl modules && uv run pd3-etl pubs"
  }
}
