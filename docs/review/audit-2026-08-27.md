# Codebase audit — 2026-08-27

Four parallel reviews (web state/lib, web UI, engine, ETL/data/CI), collated and verified against source.
Baseline before fixes: typecheck clean; web 51/51, engine 12/12, etl 30/30 passing.

Status legend: ✅ fixed in this round · 🔜 next round · ⏸ needs data/decision · ⚪ backlog

## Tier 1 — bugs (verified)

| # | Where | Finding | Status |
|---|---|---|---|
| 1 | `engine/src/po-model.ts:63` | `Model` rebuilds the channel universe from `instrument.channels` alone, dropping `extraMetals` (Cd opt-in) — the UI advertises 45 IMC channels the engine never fills. Root cause: "can an antibody sit here?" implemented three ways (`canCarryAntibody`, `Model`, web `antibodyChannel`). | ✅ |
| 2 | `engine/src/po-model.ts:117`, `index.ts:29` | Two rows locked on one mass (or an `evaluate()` assignment with a collision) are both placed, spill zeroed, no warning. | ✅ |
| 3 | `engine/src/problem.ts:81` | Duplicate row ids accepted; `Result.assignment` silently collapses them. | ✅ |
| 4 | `engine/src/problem.ts:57,66` | Unknown modality → cryptic TypeError; unknown reserved-role name silently un-reserves DNA/viability. | ✅ |
| 5 | `engine/src/problem.ts:73` | `unreserve` clears `reserved` but not `flagged` → spurious `flagged_channel` on released masses (hits `validate-kits`). | ✅ |
| 6 | `web/lib/engine-client.ts:20` | Worker crash never settles pending promises: UI stuck at "checking…" forever; no timeout; repeated `initEngine` leaks workers. | ✅ |
| 7 | `web/components/ui.tsx` | `Button` has no default `type`, so **Cancel** submits the save-panel form and the accept-warning form (the latter files an "accepted" note with no reason). | ✅ |
| 8 | `web/components/BalanceStep.tsx:165…` | Warning cards keyed by index while holding local state (preview/trials); re-balance re-binds a card to a different warning. | ✅ |
| 9 | `web/lib/store.ts:188`, `url.ts:86` | Unreadable share link is indistinguishable from "no hash": user silently gets their old draft. | ✅ |
| 10 | `web/lib/url.ts:50`, `data.ts:68` | Decoded `instrumentId`/species/sample never validated; `Index.instrument()` is a `!` → crash on first render for a stale link. | ✅ |
| 11 | `web/lib/url.ts:63,103` | `PanelRow.custom` not encoded; re-derived as `!resolved`, so a catalogue row pinned to an unsold metal loses its custom status (and its suppressed warning) across a round trip. | ✅ |
| 12 | `web/lib/store.ts:214` | Modality switch keeps `blocked` and `extraMetals` from the previous modality. | ✅ |
| 13 | `web/lib/saved.ts:26` | Quota / private-mode save failure swallowed; UI reports success. | ✅ |
| 14 | `web/components/OrderStep.tsx:28` | CSV download revokes the object URL synchronously, anchor never attached; `email` field is dead. | ✅ |
| 15 | `web/components/BrowseAll.tsx:195` | Remove assumes row id === target id. | ✅ |
| 16 | `web/components/BalanceStep.tsx:250,309` | Prose still says "half tolerance / 1×" and the "resolves this" badge tests `< 1` while thresholds are 1×/2×. | ✅ |
| 17 | `web/components/BalanceStep.tsx:82` vs `health.ts:111` | Two headline computations that can disagree on the must-fix count. | ✅ |
| 18 | `etl/pd3_etl/catalog.py:163` | `Application`/`Assay Type` classified by `startswith`; a label change silently reclassifies 372 IMC SKUs. | ✅ |
| 19 | `etl/pd3_etl/catalog.py:226` | Alias order for case-only ties follows `set` iteration → non-reproducible `catalog.json`. | ✅ |
| 20 | `etl/pd3_etl/catalog.py:240`, `modules.py:130,142` | Unguarded `int(re.match(...).group())` / `int(abundance_factor)` crash without row context. | ✅ |
| 21 | `etl/pd3_etl/modules.py:66,96` | `parse_list` swallows errors to `[]`; unmatched `kit-overrides.yaml` keys ignored on both sides. | ✅ |
| 22 | `etl/pd3_etl/pubs.py:28,110` | Personal absolute path as default DB; sqlite connection never closed. | ✅ |
| 23 | `etl/pd3_etl/instruments.py:32` | Data check is a bare `assert`. | ✅ |
| 24 | `.github/workflows/ci.yml` | `data/build` committed but never regenerated/diffed in CI; `validate-kits` never run and cannot fail; Python unpinned; `npm run data` omits `pubs`. | ✅ |
| 25 | `etl/tests/test_catalog.py`, `test_modules.py` | Assert against the committed artefact, not `build()`. | ✅ |
| 26 | `docs/TECHNICAL.md`, `README.md`, `ROADMAP.md` | Test counts, bundle sizes/counts, `docs/SPEC.md` link, `data/overrides/` all wrong. | ✅ |
| 27 | `web/lib/health.ts:99,107` | Unparenthesised `&&`/`\|\|` mix; numeric `\|\|` where `max` is meant. | ✅ |
| 28 | `web/lib/bom.ts:48` | µL-only suspension SKUs quantise to qty 1 whatever the sample count; test skips exactly those rows. | 🔜 (needs a µL→tests rule from SBT; line now carries a "size by hand" note) |

## Tier 2 — architecture seams for the roadmap (store, login, pdv2, landing pages)

| # | Seam | Finding | Status |
|---|---|---|---|
| A | Catalogue version | Bundle `version` strings are hand-typed and already stale; nothing in the app reads them; saved panels / share links carry no catalogue stamp, so a rebuild silently degrades them (raw slug rendered as a marker name). | ✅ version stamped into encoded state + saved records; drift reported as a notice; ETL bundle versions are now `date.<content-hash>` and CI diffs `data/build` |
| B | Stable IDs | Kit-module ids are slugs of the display name (`kit-overrides.yaml: name`); target ids are union-find roots (spelling-order dependent); `Conjugate.id` exists but the web never uses it. | ✅ kit ids are a hand-owned `id:` in `kit-overrides.yaml` (`kit-<pdv2 kit_id>`; 201508 is reused by pdv2 and disambiguated by hand), `slug` is display/routing; target ids pinned by `data/curated/target-ids.yaml` (`pin_target_ids`, `--update-ledger`); web `Index.module()/moduleId()` translate legacy slugs in old links; `Conjugate.id` still unused ⚪ |
| C | Panel document | `UrlState` mixes panel data with UI state (`balanced`, `nSamples`), no schema version/migrate step; saved panels are opaque encoded blobs. | ✅ `PanelDoc` type + `catalogVersion`; encode/decode are codecs over it |
| D | Persistence port | Store imports `localStorage` functions directly, synchronously. | ✅ `PanelStore` interface (async) with `LocalPanelStore`; store actions async |
| E | Order payload | Kits/custom/accepted-spill derived inside `OrderStep`; kit lines have no SKU; `BomLine` has no numeric mass. | ✅ `buildOrder()` in `lib/bom.ts` returns `{lines, kits, custom, accessories, accepted}`; kit part numbers ⏸ (ETL ask) |
| F | Designer seed | `init` reads `window.location.hash` itself; `/panels/<slug>` routes have no entry point. | ✅ `init(bundles, seed?)`; hash read moved to `page.tsx` |
| G | Engine boundary | `Problem`/`Result` unversioned; unassigned rows explained only in prose (no `blockedBy`); `index.ts` exports every internal. | ✅ `Problem.bundleVersion`, `Result.engineVersion`, `blockedBy` on unassigned, `duplicate_lock` / `invalid_assignment` codes, `stats.converged`; public surface trimmed 🔜 |
| H | Bundle validation | Web casts fetched JSON straight to types. | ✅ `assertBundles()` |
| I | Web slimming lives in `web/scripts/build-data.mjs` | A future API would re-implement it. | ⚪ move to ETL as `data/build/web/` when the backend starts |
| J | Step routing | Hard-coded in five places across `Designer`/`store`. | ✅ `lib/steps.ts` (STEPS, `stepEnabled`, `nextStep`, `landingStep`); Designer binds ids to views |

## Tier 3 — duplication and consistency

- Channel label lookup ×5 (`channelLabel` exists) ✅; reserved-mass derivation ×6 → `reservedChannels()` / `reservedMasses()` ✅ lib + components; spill tone ×4 → `spillTone()` ✅ heat map, 🔜 strip / table / sidebar pills; abundance banding duplicated from `engine/prior.ts` ✅.
- `health.ts` hands out Tailwind colour names 🔜; `MassStrip`/`SpillTable`/`WarnCard` mix props and store access ✅ convention settled (state via props, store actions bound; TECHNICAL.md); `BalanceStep` 426 lines → split `WarnCard`/`HeatMap`/`EngineError` ✅ (257 lines).
- Engine tuning constants → `engine/src/tuning.ts` ✅; `evaluate()` builds `Model` twice ✅.
- ETL `print` → `logging` ✅; curated YAML `version:` unread, no schema/duplicate-id check ✅ (duplicate-id + overrides coverage asserted).

## Tier 4 — accessibility (roadmap §7)

Mass strip mouse-only and colour-only; search dropdown not a combobox, no Escape; `focus:outline-none` on the three busiest controls; nested `role="button"` inside a `<button>` on mobile; four overlay idioms none of which trap focus. 🔜 as one pass.

## Tests added this round

- engine: extraMetals honoured; duplicate locks / ids / evaluate collisions; unknown role/modality; unreserve clears flagged; 1×/2× thresholds at the boundary.
- web: worker error/timeout rejects and sets `engineError`; malformed hash → notice; unknown instrument id → default; `custom` bit round-trip; catalogue-version drift notice; save failure surfaces.
- etl: `build()` exercised directly; unknown Application raises; alias order total.
- CI: ETL regen diff gate; `npm run validate --check`; Python 3.12 pinned.

## Result of this round (2026-08-27)

All suites green after the fixes: etl 40 pytest (was 30), engine 24 vitest (was 12) + `validate:check` 45 better / 16 equal / 0 worse, web 64 vitest (was 51) + 8 Playwright, `next build` clean.
`data/build` regenerated: byte-identical on re-run; the only content changes are two new (empty) QA buckets in `stats` and three alias re-orders.
Not committed — review the working tree, then commit as one "audit round" or per package.

Carried to the next round, in priority order: (1) ✅ stable kit ids + target-id ledger (Tier 2 B, done 2026-08-27 after the commit of the round); (2) ✅ step registry + `BalanceStep` split (J, Tier 3); (3) accessibility pass (Tier 4); (4) engine public-surface trim; (5) e2e for the over-budget flow and the accept-and-order flow.
